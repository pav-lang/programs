
func main():
    // array (fixed size)
    l = [1, 2, 3]
    l[0] = 4
    l2 = [0; 10] // ten zeros

    // tuple
    t = {1, 2.2, true}
    t[0] = 3

    // list
    l = list()
    l.insert(5)
    l.insert(1, 6)
    println(l.size())

    // stack
    s = stack()

    // queue
    q = queue()

    // map
    m = map()
    m.insert(1, "a")
    m.insert(22, "b")
    println(m.get(22))